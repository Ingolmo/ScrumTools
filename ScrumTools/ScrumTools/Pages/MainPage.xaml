<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ScrumTools"
             x:Class="ScrumTools.Pages.MainPage"
             Title="Scrum Tools">
    <ContentPage.ToolbarItems>
        <ToolbarItem 
            Name="Settings"
            Command="{Binding GoToSettingsCommand}"
            Order="Primary" Priority="0" Icon="icon" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <Grid
            RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image 
                Grid.Row="0"
                Source="icon"/>
            <ScrollView 
                Grid.Row="1" 
                Padding="5"
                BackgroundColor="Gray">
                <StackLayout
                    Spacing="5">
                    <ContentView
                        BackgroundColor="Red"
                        HeightRequest="100">
                        <ContentView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding GoToCardsGridCommand}" />
                        </ContentView.GestureRecognizers>
                        <Image
                            Source="icon"
                            />
                    </ContentView>
                    <ContentView
                        BackgroundColor="Red"
                        HeightRequest="100">
                    </ContentView>
                    <ContentView
                        BackgroundColor="Red"
                        HeightRequest="100">
                    </ContentView>
                    <ContentView
                        BackgroundColor="Red"
                        HeightRequest="100">
                    </ContentView>
                    <ContentView
                        BackgroundColor="Red"
                        HeightRequest="100">
                    </ContentView>
                </StackLayout>
            </ScrollView>
        </Grid>
        
    </ContentPage.Content>
	
       

</ContentPage>